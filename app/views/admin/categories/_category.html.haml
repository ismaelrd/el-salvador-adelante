%tr
  %td
    = category.id
  %td
    = category.name
  %td
    = category.created_at.try(:strftime, '%d/%m/%Y')
  %td
    = link_to edit_admin_category_path(category), class: 'btn btn-primary btn-xs' do
      %i.glyphicon.glyphicon-edit
      = t('labels.edit')
